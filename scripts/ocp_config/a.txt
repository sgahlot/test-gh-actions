oc create serviceaccount github-actions -n $NAMESPACE

# 2. Grant edit role
oc adm policy add-role-to-user edit -z github-actions -n $NAMESPACE

# 3. Create token secret
oc apply -f sa_token_sceret.yml

# 4. Grant serviceaccount specific permissions to manage RBAC resources
oc apply -f sa_token_sceret.yml

# 5. Extract token
OCP_TOKEN=$(oc get secret github-actions-token -o jsonpath='{.data.token}' -n $NAMESPACE | base64 --decode)